/**
 * Animation App Root
 *
 * This is the starter template. The animation agent will overwrite this
 * file with the actual animation code (Theatre.js + React Three Fiber).
 */

export default function App() {
  return (
    <div
      style={{
        width: '100vw',
        height: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        background: '#000',
        color: '#fff',
        fontFamily: 'system-ui, sans-serif',
      }}
    >
      <p>Animation will render here.</p>
    </div>
  );
}
